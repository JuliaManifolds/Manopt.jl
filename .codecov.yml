ignore:
  - "src/documentation_glossary.jl" # exclude this since it is just used to create the docs and code cov goes bogus on this.
  - "src/helpers/test.jl" # exclude tests since they are not part of the package code but merely helpers